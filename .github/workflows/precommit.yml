name: Precommit checks

on:
  precommit:
    branches: [master]

jobs:
  precommit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Instalar pre-commit
        run: pip install pre-commit
      - name: Correr pre-commit checks
        run: pre-commit run --all-files
